<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <title>SALUS - Sign-In</title>

    <link rel="stylesheet" href="assets/vendor/bootstrap/css/bootstrap.min.css">
    <link href="assets/vendor/fonts/circular-std/style.css" rel="stylesheet">
    <link rel="stylesheet" href="assets/libs/css/style.css">
    <link rel="stylesheet" href="assets/vendor/fonts/fontawesome/css/fontawesome-all.css">
    <link rel="stylesheet" href="salus/css/input-check.css">
    <style>
        html,
        body {
            height: 100%;
        }

        body {
            display: -ms-flexbox;
            display: flex;
            -ms-flex-align: center;
            align-items: center;
            padding-top: 40px;
            padding-bottom: 40px;
            background-image: linear-gradient(to top, #7f7fd5, #86a8e7, #91eae4);
            background-repeat: no-repeat;
            background-attachment: fixed;
            font-family: 'Arial'
        }

        h2.logostr {
            margin: 0 auto;
            letter-spacing: 4px;
            color: #1674b3;
        }

        h3 {
            font-family: 'Arial';
            font-weight: bold;
        }

        .img-responsive.img-center {
            padding-top: 16px;
            margin: 0 auto;
        }

        #signin-btn {
            background-color: #3498DB;
            color: #ffffff;
        }
    </style>
</head>

<body>
    <form class="splash-container" action="/signup/handle" method="post" oninput="passwordc.setCustomValidity(passwordc.value != password.value ? 'Passwords do not match.' : '')">
        <div class="card">
            <img src="assets/images/logo.png" class="img-responsive img-center" width="64">
            <h2 class="logostr">SALUS</h2>
            <div class="card-header">
                <h3 class="mb-1">Sign-In</h3>
                <p>Please enter your user information.</p>
            </div>

            <div class="card-body">
                <div class="form-group">
                    E-mail <input class="form-control form-control-lg" type="email" name="email" required="" autocomplete="off">
                </div>
                <div class="form-group">
                    Password <input id="password" class="form-control form-control-lg" type="password" name="password" required="" pattern="^(?=.*\d)(?=.*[a-z])(?=.*[A-Z]).{8,32}$">
                </div>
                <div class="form-group pt-2">
                    <button class="btn btn-block btn-primary" id="signin-btn" type="submit">Sign-In</button>
                </div>
            </div>
            <div class="card-footer bg-white ">
                <a href="/forgot_password" class="text-secondary float-right">Forgot your password?</a>
            </div>
        </div>
    </form>

    <script type="text/javascript" src="salus/js/sign-up.js"></script>
</body>
</html>